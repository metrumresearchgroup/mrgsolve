% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/complete_tran.R
\name{complete_tran}
\alias{complete_tran}
\title{Complete nmtran-like data item columns}
\usage{
complete_tran(data, ..., case = c("lower", "upper", "preserve"), id = TRUE)
}
\arguments{
\item{data}{a data frame.}

\item{...}{not used; for future extensions.}

\item{case}{output case for columns that are added; use \code{"preserve"} to
infer from existing nmtran-like names in \code{data}.}

\item{id}{if \code{TRUE}, add \code{ID} / \code{id} when missing.}
}
\value{
A data frame with missing nmtran-like columns added.
}
\description{
Add common nmtran-like data item columns to a data frame using conservative
default values. Existing columns are not modified, and row order is
preserved.
}
\details{
This function does not validate whether the resulting data set is
scientifically meaningful for simulation; it only fills absent standard
columns with default values.

Default values for added columns are:
\itemize{
\item \code{ID} / \code{id}: \code{1} (only when \code{id = TRUE})
\item \code{TIME} / \code{time}: \code{0}
\item \code{AMT} / \code{amt}: \code{0}
\item \code{CMT} / \code{cmt}: \code{1}
\item \code{EVID} / \code{evid}: \code{0}
\item \code{RATE} / \code{rate}: \code{0}
\item \code{II} / \code{ii}: \code{0}
\item \code{ADDL} / \code{addl}: \code{0}
\item \code{SS} / \code{ss}: \code{0}
}
}
\examples{
complete_tran(data.frame(time = c(0, 12, 24)))

complete_tran(data.frame(time = c(0, 12), amt = c(100, 0), cmt = 1))

complete_tran(data.frame(TIME = 0), case = "upper")

complete_tran(data.frame(time = c(0, 24)), id = FALSE)

}
