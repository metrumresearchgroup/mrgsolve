library(pkgdown)
library(methods)
build_site(".", path = "DOCS")
# init_site(".", path = "DOCS")
# build_articles(".", path = "DOCS")
# build_home(".", path = "DOCS")
# build_reference(".", path = "DOCS")

